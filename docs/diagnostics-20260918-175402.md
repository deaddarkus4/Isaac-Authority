# Проверка без воспроизведения desync, 18 сентября 2026

Источник: `IsaacDiagnostics-20260918-175402-95666310.zip`, собран в 17:54:02 UTC. Пользователь сообщает, что принудительного выброса из лобби не было.

SHA256 архива: `9544746dd55ae4ea343ec48ee16b169dfc15283a90728a8e1770755aa1e25eea`. CRC всех файлов проверены.

## Условия

- J460 / Steam build 22878971, оригинальный EXE: `3bdfc8bae0dc7e334b76009d0ad45dfbb16ee5f00c06ffbc3a0094e34d44616b`.
- Коллектор не обнаружил модов; `EnableMods=1` сам по себе не означает наличие загруженных модов.
- `OnlineInputDelay=3`, `EnableDebugConsole=0`, `EosCrossplay=0`, `VSync=1`.
- Архив описывает один клиент. Он не подтверждает конфигурацию остальных участников.

## Результат записи

Три JSONL содержат 212, 207 и 2570 записей. Все строки валидны, последовательности событий непрерывны, максимальный `droppedTotal=0`, обрезанных сообщений нет. Все три процесса завершились сообщением `Isaac has shut down successfully`.

Основной журнал `session-20260918-174126-13824-1141516078.jsonl` охватывает 744 секунды между первым и последним записанными событиями. В нём 21 сообщение `RNG Start Seed`, 83 сообщения категории `room`, 9 категории `collectible` и 5 категории `item_queue`. Это счётчики сообщений, а не число уникальных комнат или гарантированно отдельных подборов.

Нет зарегистрированных подборов тринкетов, в частности Vibrant Bulb. Исторический seed `KAKG ZR44` не запускался. Таким образом, [ранее наблюдавшаяся цепочка с лампочкой](desync-test-scenario.md) здесь не проверена.

Запись категории `desync` ровно одна: sequence 2318, `NetDesyncHandler::RemoveUser()`. Перед ней на кадре 122 идут `left lobby`, постановка удаления игрока в очередь и `RemoveRemotePlayer`. Это имя обработчика удаления участника, не сообщение об обнаружении рассинхронизации. По этой записи нельзя определить причину выхода. Сообщений о фактическом расхождении состояния в предоставленных журналах не найдено.

Три встроенных журнала online sessions содержат накопленный текст (1, 3 и 21 старт соответственно); их нельзя суммировать как независимые попытки.

## Вывод

Это контрольная запись без подтверждённого desync, а не подтверждение исправления. Регистратор работал и сохранил события игры без зарегистрированных потерь; он не записывает полное состояние симуляции или сырые вводы. Для следующего разбора полезнее запись обоих клиентов вокруг конкретного проблемного действия, чем повторение случайных стартов. Подготовка гарантированного онлайн-сценария с предметами в стартовой комнате пока не реализована.
