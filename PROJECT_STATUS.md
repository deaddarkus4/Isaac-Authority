# Статус проекта — 20 сентября 2026

Этот файл — точка продолжения работы. Подробные исследования лежат в `docs/`; старые документы могут описывать более ранний этап.

## Текущее состояние — 20 сентября

**Этап «настоящий переход комнат» (0.5.0-experimental) пройден в живой игре 19 сентября, 21:32–21:34 UTC, по обоим маршрутам.** Подробности и таблицы: [переход комнат](docs/j460-room-transition.md).

- `door`: HOST проходит в дверь 84 → 97, реплику в ту же комнату проносит **собственная дверь игры-реплики** (игрок удерживается в проёме в позиции хоста; подавление обновления игрока двери не отключает). Модуль принимает это как новое место и продолжает: 188 снимков до двери, 226 после, слёзы по обе стороны.
- `request`: 11 снимков у двери задержаны, реплика остаётся вне проёма и сама вызывает `Game::StartRoomTransition` — **первый вызов этой функции в живой игре**, ABI подтвердился; комната достигнута за 328 мс. 189 снимков до двери, 204 после.
- В обоих: все состояния совпали с кадрами хоста побитово как float32, создано 4 / удалено 4 копии слёз, ошибок и отклонённых пакетов 0, слоты восстановлены. Summary и SHA256 — в `IsaacAuthority/Room/installation.json` (`liveTestVerified=true`).

До этого три прогона не прошли (пауза HOST, игрок не по оси двери, остановка модуля при собственном переходе реплики); каждый дал исправление. Проверен один переход через одну дверь; обратный путь, несколько переходов, большие комнаты, телепорты не проверялись. Ветки `settled` и `vanished` в живых прогонах не сработали.

**Один seed не даёт одинаковых уровней при разных сохранениях:** у HOST и REPLICA с `CZH4 8E6W` различаются 5 комнат из 15 (83, 85, 109, 126, 138). Нижняя дверь стартовой комнаты (97) общая, правая — нет. Тест требует совпадения маршрута, а не уровня; реплика нативно сверяет каждую комнату назначения. Для полного совпадения клиенту нужно сохранение хоста.

**Состояние игр.** Обе игры (HOST PID 42680, REPLICA PID 12800) стоят в стартовой комнате забега с **введённым** seed `CZH4 8E6W` — такой забег сохраняет seed при перезапуске, поэтому дальнейшие прогоны не требуют ручных действий:

```powershell
python scripts/Test-IsaacRoom.py --source-pid <HOST_PID> --replica-pid <REPLICA_PID> --restart-runs --check
python scripts/Test-IsaacRoom.py --source-pid <HOST_PID> --replica-pid <REPLICA_PID> --restart-runs --route door --binary-directory '<папка игры>\IsaacAuthority\Room'
python scripts/Test-IsaacRoom.py --source-pid <HOST_PID> --replica-pid <REPLICA_PID> --restart-runs --route request --binary-directory '<папка игры>\IsaacAuthority\Room'
```

Тест около 12 секунд управляет окном HOST (стрельба, выравнивание по двери, проход, стрельба за дверью); `--restart-runs` перед этим перезапускает оба забега (R, на экране смерти — Space). **Состояние окон — главный источник сбоев теста:** HOST при потере фокуса открывает меню паузы и держит его; REPLICA не останавливается никогда и в комнате с врагами погибает; на экране смерти Esc выводит в главное меню (так промежуточная версия теста однажды вывела REPLICA в меню — забег восстановлен через меню, тест теперь шлёт Esc только при живом игроке). Тёмный экран HOST — проклятие темноты, а не пауза; паузу определять по счётчику кадров `Game+0x264f8`. После каждого прогона игры остаются в комнате с врагами — возвращать их `--restart-runs --check`.

В новой сессии заново определить процессы и состояние. В процессах загружены DLL этапов Pair и World и две версии модулей комнат (первая — под простыми именами, неактивна). Новые сборки устанавливаются под именами с хешем содержимого и загружаются рядом без перезапуска игр.

Рабочие точки входа:

- `native/IsaacAuthority/src/world_adapter.cpp` — источник, создание/удаление и удержание слёз, проверка контекста, очистка на игровом потоке. С флагом `ISAAC_WORLD_ROOMS` — поиск комнаты в уровне, запрос перехода, фаза «в пути».
- `native/IsaacAuthority/src/world_state.cpp` и `world_receiver.cpp` — полный набор сущностей, ID, epoch, формат WLD1, свежесть и приём UDP; `SamePlace`, `Decide`, `Direction` для перехода.
- `scripts/Test-IsaacRoom.py` и `isaac_level.py` — живой тест перехода, read-only сравнение уровней, текст seed, выбор двери.
- `reverse/ghidra/ExportIsaacRoom.java`, `Binaries/research/j460-room*/`, `native/IsaacNetProbe/profiles/j460-room.json` — разрешение имён Lua API в нативные функции, найденные адреса перехода и seed.
- `scripts/Test-IsaacWorld.py` и `isaac_world.py` — живой тест, контрольные сетевые события, сверка с исходными кадрами; `--probe` отдельно проверяет один нативный spawn/update/remove.
- `scripts/Start-IsaacReplica.ps1` и `native/IsaacAuthority/src/isolated_launch.cpp` — второй экземпляр с отдельным профилем. `savedatapath.txt` только информирует о пути; изоляция задаётся в памяти нового приостановленного процесса, EXE на диске не изменяется.
- `reverse/ghidra/ExportIsaacWorld.java`, `Binaries/research/j460-world/`, `native/IsaacNetProbe/profiles/j460-world.json` — найденные функции и проверенные поля J460. В частности, путь изменения комнаты RVA `0x2fe2f0` изучен статически, но ещё не вызывался и не проверялся в живой игре.

Последний воспроизводимый результат: [отчёт по слезам](docs/j460-world-tears-test.md), summary `Binaries/diagnostics/world/world-20260919-203818-378209.summary.json`, SHA256 `f85ec8b42255ad01631efb7066021fcca197f5fc4aeb0382fdc0fe6cfb6135aa`. Установка 0.4 находится в `<папка игры>/IsaacAuthority/World`, её `installation.json` содержит хеши файлов и успешный результат.

Команды для проверки после следующих изменений:

```powershell
.\scripts\Build-IsaacAuthority.ps1
python -m unittest discover -s scripts/tests -p test_*.py
```

Последний результат — 10/10 CTest и 21/21 Python-тест (20 сентября). Не запускать старые живые тесты только ради повторного подтверждения. Автоматические сценарии управляют окном HOST и нажимают клавиши: перед новым живым прогоном проверить нужный одиночный забег и предупредить пользователя. Установщики и тесты предыдущих этапов сохранены; для текущего этапа использовать Room (`IsaacAuthority/Room`, `Test-IsaacRoom.py`), не путать его с пакетами World/Network/Pair.

## Цель и принятое направление

Пользователь выбрал **замену сетевой модели Isaac на авторитетный хост**: хост считает мир, клиенты предсказывают движение и корректируются по его состоянию. Временное расхождение не должно разделять лобби, задержка клиента не должна останавливать хост. Исправление отдельных случаев старого lockstep не является основным направлением; сохранённые десинки нужны как проверочные случаи.

**Полноценного альтернативного мультиплеера в игре пока нет.** Между двумя настоящими J460 проверены реплика игрока, жизненный цикл обычных слёз (создание, состояние, удаление) и настоящий переход через дверь в ту же комнату. Добавлены ID сущностей и защита от пакетов старой комнаты. Враги, предметы и боевая логика пока не синхронизированы.

## Сделано и проверено

| Компонент | Фактический результат |
| --- | --- |
| Совместимость патчера | Исходники 1.4.0 распознают семь областей патчей оригинальной J460. Изменение сигнатур для неё не потребовалось. |
| Диагностика | IsaacNetProbe записывает debug-output игры в JSONL. Работоспособность подтверждена реальными забегами и десинками. Сырой ввод и полное состояние мира не записывает. |
| Поиск установки | Steam registry + основные/дополнительные библиотеки, modern/legacy libraryfolders.vdf, manifest, ручной выбор при неоднозначности. 13 проверок прошли. Диск C не обязателен. |
| Сетевой стенд | `native/IsaacAuthority`: авторитетное движение, prediction/reconciliation, ограниченные очереди, отсечение старых/повторных пакетов, UDP localhost. Хост продолжает шаги при остановке клиента; после восстановления связи состояния сходятся. Физика стенда упрощённая. |
| Чтение J460 | Найдены указатели игроков, положение/скорость и кольцевая история кадров. Проверено внешнее чтение запущенного оригинального EXE без записи в память. Восемь тестов парсера/гонок/пакетов прошли. |
| Игра → сетевой наблюдатель | Реальные позиции/скорости двух игроков переданы в отдельный C++-процесс. 136 снимков приняты; конечные float32 совпали побитово. Это наблюдатель, не второй игровой клиент. |
| Эксперимент коррекции | **Живой одиночный тест J460 прошёл 19 сентября.** Наблюдение: 127 обновлений без коррекций. Второй тест: 244 обновления, ровно один сдвиг X на +8, следующее обновление сохранило новую позицию. Поток 12600, PID 40064; оба раза слот восстановлен, потерь записей нет. |
| Сетевое применение состояния | **Живой тест прошёл 19 сентября, 19:26 UTC.** `IsaacAuthorityNetwork.dll` принял 5 состояний от отдельного Python-хоста по UDP loopback, применил их на потоке 43720 в PID 21580, отклонил 8 пакетов. 96 обновлений, все float32 и следующие обновления проверены; позиция возвращена к исходной, слот восстановлен. Это тестовый отправитель, не второй Isaac. |
| Две реальные игры | **Живой тест прошёл 19 сентября, 19:57 UTC.** Хост PID 42680: 622 обновления. Реплика PID 12800: 619 обновлений, 271 применение с точным float32-соответствием исходному кадру, 605 собственных обновлений игрока пропущены. За секунду без пакетов хост выполнил 60 обновлений и двигался, реплика удерживала состояние; после восстановления передачи сошлась с хостом. Слоты обеих игр восстановлены. |
| Жизненный цикл слёз | **Живой тест прошёл 19 сентября, 20:38 UTC.** 194 применённых снимка, 3 созданных и 3 удалённых копии, остаток 0. Все поля совпали с источником как float32. За паузу передачи хост выполнил 60 обновлений, клиент удержал живые слёзы в 41 проверенном обновлении. Контрольный новый epoch удалил последнюю копию, 3 старых пакета отклонены. Реального перехода комнаты в этом тесте не было. |
| Переход комнат | **Живой тест прошёл 19 сентября, 21:32–21:34 UTC, по двум маршрутам.** HOST проходит в настоящую дверь 84 → 97. `door`: реплику проносит её собственная дверь, модуль продолжает в новой комнате (188 + 226 снимков). `request`: 11 снимков у двери задержаны, реплика сама вызывает `StartRoomTransition` и достигает комнаты за 328 мс (189 + 204 снимка). Все состояния совпали как float32, слёзы реплицированы по обе стороны двери, ошибок 0. Один переход через одну дверь; при одном seed и разных сохранениях различаются 5 комнат из 15. |

Последняя сборка Authority: MSVC x86 Release, `/W4 /WX`, **10/10 CTest и 21/21 Python-тестов** прошли 20 сентября. Подробности: `Binaries/authority-build/Testing/Temporary/LastTest.log`. Последняя живая проверка: [настоящий переход комнат](docs/j460-room-transition.md). Предыдущие этапы: [жизненный цикл слёз](docs/j460-world-tears-test.md), [реплика игрока в двух играх](docs/j460-game-pair-test.md).

## Текущая установка и артефакты

- Игра: `C:\Program Files (x86)\Steam\steamapps\common\The Binding of Isaac Rebirth`.
- Проверяемая версия: Repentance+ `v1.9.7.17.J460`, Steam build `22878971`, патч 21 апреля 2026.
- EXE оригинальный, SHA256 `3bdfc8bae0dc7e334b76009d0ad45dfbb16ee5f00c06ffbc3a0094e34d44616b`. Патчи разрешения модов/аналитики сейчас не применены.
- Установлен пакет диагностики **0.2.1** в подпапке игры `IsaacNetProbe`. Запись включается ярлыком **Isaac Diagnostics**; обычный запуск через Steam сам по себе её не включает.
- Пакет другу: `Binaries/IsaacNetProbe-0.2.1-win-x86.zip`. Не путать с `Binaries/IsaacOnlineModded-1.4.0-win-x64.zip` — это отдельный старый по назначению патчер модов.
- Native-журналы: `%LOCALAPPDATA%\IsaacNetProbe\logs`; журналы игры: `Documents\My Games\Binding of Isaac Repentance+\online_logs`.
- Authority 0.1.0-experimental установлен в подпапку игры `IsaacAuthority`: DLL, attach EXE, тестовый скрипт и `installation.json`. Установленный скрипт берёт бинарники рядом с собой и копирует проверенные журналы в `%LOCALAPPDATA%\IsaacAuthority\verified-reports`. После теста хук отключён; DLL остаётся загруженной до выхода из игры. Автозапуска нет. Исходные бинарники: `Binaries/authority-build/Release/`.
- Сетевой эксперимент 0.2.0-experimental установлен отдельно в `IsaacAuthority/Network`; установщик — `scripts/Install-IsaacAuthorityNetwork.ps1`. Автозапуска нет. Живая проверка новой DLL прошла; `installation.json` содержит результат и SHA256 summary. Для обновления загруженной DLL требуется перезапуск игры.
- Пара игровых модулей 0.3.0-experimental установлена в `IsaacAuthority/Pair`: Source DLL, Replica DLL, attach EXE и проверочный скрипт. Живой тест прошёл, хеш summary сохранён в manifest. Установщик — `scripts/Install-IsaacGamePair.ps1`.
- Модули обычных слёз 0.4.0-experimental установлены в `IsaacAuthority/World`: WorldSource/WorldReplica DLL, attach EXE, скрипты и codec. Установщик — `scripts/Install-IsaacWorld.ps1`, тест — `scripts/Test-IsaacWorld.py`. Manifest содержит результат живого теста; оба перехвата игрока/слезы после проверки восстановлены, созданные копии удалены.
- Модули перехода комнат 0.5.0-experimental установлены в `IsaacAuthority/Room`: RoomSource/RoomReplica DLL, attach EXE, `Test-IsaacRoom.py`, `isaac_level.py` и зависимости. Установщик — `scripts/Install-IsaacRoom.ps1`; DLL устанавливаются под именами с хешем содержимого, имена — в `installation.json`. Manifest содержит оба пройденных живых прогона с SHA256 их summary.
- Второй экземпляр создан `scripts/Start-IsaacReplica.ps1`: рабочая папка `Binaries/game-instances/replica-414553eb`, отдельный профиль `Documents/My Games/IsaacAuthority-414553eb`, локальные сохранения, SteamCloud=0 и PauseOnFocusLost=0. Окна помечены HOST / REPLICA. После теста оба процесса работают обычным образом, перехваты отключены; DLL остаются до выхода.

## Последний десинк: уточнение пользователя

**Десинк произошёл ровно при подборе Vibrant Bulb, не при переходе в комнату.** Возврат в соседнюю комнату и разделение лобби — последующие события. Пользователь отдельно уточнил этот порядок; не трактовать его сообщение как сбой перехода.

- Сессия `09_18_2026__19_37_25__Игрок 1`, seed **FCNQ KN1J**, три участника.
- Кадр расхождения **106841**, зарегистрирован на 106844. На скриншоте Lazarus/Игрок 1 поднимает лампочку, активка полностью заряжена.
- Расходится PostVelocity Y Lazarus: локально `0.3324480652809143`, у обоих остальных `0.3035395443439484`. RNG и контрольные суммы общего сохранения совпадают.
- Сообщение `Adding trinket 100` печатается позже при сбросе очереди; оно не переносит момент подбора на время после десинка.
- Архив с логами, скриншотом и разбором: `Binaries/diagnostics/vibrant-bulb-latest/IsaacDiagnostics-20260918-204736-2804ebc8.zip`.
- SHA256 архива: `4f99cbe129c994f7fb7442a64548b0882b4ed97c7f790de7d64be94b1b53491a`.
- [Подробный разбор](docs/vibrant-bulb-desync-20260918-204736.md). В архиве сохранена версия разбора на момент сбора; последующее уточнение пользователя отражено в документах проекта.

Другие сохранённые случаи: [два совпадающих десинка на кадре 38 в стартовой комнате](docs/paired-desync-20260918-180254.md), [ранний Vibrant Bulb, seed KAKG ZR44](docs/vanilla-vibrant-bulb-desync.md). Причина внутри расчёта движения пока не доказана. Гарантированный короткий игровой сценарий/принудительный seed в официальном онлайне не реализован.

## С какого шага продолжать

Пользователь подтвердил готовность; наблюдение и `-CorrectOnce` **успешно выполнены**. Журналы скопированы в `Binaries/diagnostics/player-step/`: `player-step-40064-11572156.jsonl` и `player-step-40064-11580375.jsonl`.

Реализован, установлен и **проверен в живом J460** [сетевой эксперимент](docs/j460-network-state-test.md). Готовность пользователя получена; пять абсолютных состояний с малыми сдвигами и возвратом в исходную точку успешно применены. Summary: `Binaries/diagnostics/network-state/network-21580-20260919-192621-354447.summary.json`. Активный перехват после проверки отключён.

Настоящий источник и реплика одного игрока также **реализованы и проверены**. Итог: `Binaries/diagnostics/game-pair/pair-20260919-195656-152409.summary.json`. Передано 541 состояние, применено 271; 270 промежуточных снимков заменены более свежими в очереди. Во время паузы передачи реплика не рассчитывала движение самостоятельно, хост продолжал обновления.

Реализован и проверен жизненный цикл обычных слёз: ID учитывает адрес/seed/локальный индекс, полные снимки создают и удаляют копии, клиент не считает их движение/столкновения самостоятельно. Итог: `Binaries/diagnostics/world/world-20260919-203818-378209.summary.json`. Контрольная смена epoch проверила очистку живой копии и отсечение старых пакетов, **но не реальную загрузку следующей комнаты**.

Настоящий переход обеих игр [реализован](docs/j460-room-transition.md): реплика находит комнату хоста в собственном уровне, сверяет её и запрашивает штатный переход игры. Живой тест `scripts/Test-IsaacRoom.py` прошёл по маршрутам `door` и `request`. Согласование seed пока ручное: нативный путь через консоль (`Console::RunCommand`, RVA `0x28cdc0`) найден, но безопасная точка вызова перезапуска не установлена.

Дальше — расширение репликации на врагов, другие снаряды, предметы/урон/награды. Враги — первый кандидат: сразу за дверью у каждой игры свои враги, реплика получает собственный урон и погибает независимо от хоста, а подавить можно только обновление сущностей, но не логику комнаты (дверь сработала при подавленном обновлении игрока). Также остаются передача ввода клиента хосту, игровое предсказание и автоматическое согласование seed/сохранений. Не повторять завершённые проверки без новых изменений или конкретной причины.

[Описание и ограничения игрового теста](docs/j460-player-step-test.md). Адаптер проверяет одного локального игрока и пустой список сетевых устройств; подготовленный тест не запускать поверх текущего онлайн-забега.

## Не сделано и ограничения

- ABI/перехват и разовая запись X проверены в одном одиночном забеге J460. Смена комнаты, уничтожение игрока и длительная работа с коррекциями не проверены. Полная декомпиляция большой функции обновления игрока не завершилась за выделенный лимит.
- Две игры проверены только в пустых стартовых комнатах: loopback, один выбранный игрок, сессия до 30 секунд, пакет до 250 мс. Ограничение скачка 128 пикселей относится к модулю Pair; World имеет отдельные диапазоны WLD1 и проверку контекста комнаты. Полноценные коллизии, бои, переходы и большие коррекции не проверены.
- Реплика подавляет обновление выбранного игрока и созданных ею обычных слёз; остальные сущности мира продолжают собственную симуляцию. У копий слёз отключены столкновения и урон. Нет интерполяции и автоматического согласования seed/сохранений; смена комнаты проверена для одного перехода через одну дверь и покрывает только обычные комнаты сетки в измерении 0.
- Сетевые ID пока реализованы только для набора до 16 стандартных слёз `2.0.0` в одной комнате. Нет синхронизации врагов, других снарядов, урона, предметов, сохранений, переподключения и миграции хоста; нет интернет-транспорта/аутентификации.
- Старый внешний state reader даёт best-effort чтение. Source-модули публикуют данные игрока и поддерживаемых слёз после обновления игрока через счётчик поколений; **границы полного шага и атомарного снимка мира по-прежнему нет**.
- Старые issues, EID-миграция и прочие замечания исследованы в [issue-triage.md](docs/issue-triage.md); не считать все перечисленные проблемы исправленными.

## Состояние рабочей копии

Работа пока не закоммичена: изменены README/.gitignore, новые `PROJECT_STATUS.md`, `docs/`, `native/`, `reverse/`, `scripts/`. Пользовательская папка **`Upgrader/` не относится к этой работе, не менять и не включать автоматически в коммит**. `Binaries/` содержит локальные сборки, дампы/отчёты и исследовательские артефакты, игнорируется Git. Коммитов/публикаций/нового релиза в рамках фиксации статуса не выполнялось.

Основные ссылки: [интеграция J460](docs/authority-j460-integration.md), [живое чтение и UDP-мост](docs/j460-live-state-reader.md), [сетевой стенд](native/IsaacAuthority/README.md), [диагностический регистратор](docs/native-recorder.md).
