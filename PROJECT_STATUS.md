# Статус проекта — 21 сентября 2026

Этот файл — точка продолжения работы. Подробные исследования лежат в `docs/`; старые документы могут описывать более ранний этап.

## Передача в новую сессию — 20 сентября 2026

За прошлую сессию пройдены в живой игре четыре этапа: переход комнат, коррекция врагов, урон и смерть по решению хоста, ввод клиента на хосте со вторым игроком. **В этой сессии (20 сентября, ночь) половины сведены, три этапа подряд: [снимок мира с двумя игроками](docs/j460-coop-players-test.md), [замкнутая петля](docs/j460-closed-loop-test.md) и [предсказание своего игрока](docs/j460-prediction-test.md).** Клавиши нажимаются только в окне клиента; его модуль ввода сам опрашивает клавиатуру и шлёт команды, хост без фокуса окна симулирует игрока клиента физикой игры и возвращает мир с номером потреблённой команды; клиент ведёт своего игрока сразу той же командой, а реплика сверяет его с хостом: расхождение после одной и той же команды — медиана 0,009, максимум один шаг ходьбы (4,42), ни одного сдвига и телепорта. **Модель авторитетного хоста с предсказанием работает в настоящей J460 — в одной пустой комнате.** Затем её проверили [через плохую сеть](docs/j460-impaired-network-test.md) (задержка 100 мс, дрожание 80 мс, потери 15 % в каждую сторону): это вскрыло дефект — хост читал самую свежую команду, и сверка исправляла ошибки, которых нет, — и он исправлен **пошаговым вводом**: одна команда на шаг игрока с обеих сторон, на хосте — из буфера воспроизведения. Сетевых сдвигов стало 0 вместо 10. Наконец, по наблюдению пользователя (проклятие есть в одном окне и нет в другом) найдена и устранена причина разных уровней при одном seed — [общее сохранение](docs/j460-shared-save.md): экземпляр с копией сохранения хоста получает тот же уровень, комната в комнату, и то же проклятие. Тем же способом сделан [вход клиента в забег хоста](docs/j460-run-join.md): экземпляр, запущенный с копий сохранения и файла состояния забега, через «Continue» оказывается в том же забеге — вплоть до потраченной бомбы — и работает клиентом в полном конвейере с предсказанием. По поправке пользователя (в штатном онлайне нельзя войти посреди забега, а ограничения подстраиваются под всех игроков лобби) сделано [сохранение лобби](docs/j460-lobby-save.md): разобран формат сохранения и его контрольная сумма, инструмент объединяет сохранения участников по наименьшему значению, и игра принимает записанный им файл.

### С чего начинать

> **21 сентября 2026 — работа над входом в идущую сессию приостановлена по слову пользователя; всё сделанное за день опубликовано по его просьбе:** `python scripts/Publish-IsaacAuthority.py` → <https://github.com/deaddarkus4/Isaac-Authority> `main` = `25e0e6c` (рабочий коммит `58cfc09`; fast-forward с `d7a341a`). Релиза нет: 0.1.3 живьём проверена не вся (Needle, следование за этажом, правило `join`). Пользователь уточнил про штатный онлайн: войти в лобби идущей игры можно, входящий ждёт в лобби (до конца забега или до следующего этажа — он не помнит; по коду — до смены этажа). На момент паузы открыты три тестовые игры пробы входа (хост прошёл три комнаты первого этажа, впуск не наблюдался); основная сборка не пересобрана — пересобрать после их закрытия.
>
> **21 сентября 2026, позже — вход в идущую сессию (просьба пользователя): в игре он уже есть штатно, очередь входа заработала на стенде; впуск на смене этажа вживую ЕЩЁ НЕ наблюдался.** Документ — [j460-join-in-progress.md](docs/j460-join-in-progress.md). В J460 найдены и разобраны: признак лобби `current_stage`, диалог Quick Match «войти в идущую», очередь `pending joins` (встаёт по «готов» вошедшего у каждого участника), впуск **на смене этажа** (`0x50df40`), стартовое сообщение вида 5 «Join Existing Game» (`0x517fd0`: кадр, состав, сжатый `GameState`, общий генератор, общее сохранение). Живая проба на трёх изолированных экземплярах без нашего модуля: C вошёл в лобби идущего матча A+B, у A и B `Adding pending join`. **Правило пользователя (анлоки сессии ⊆ анлоки входящего) сделано чистой логикой** — `SharedUnlocks`/`UnlocksLacking` в `native_state` и `isaac_save.py joinable`; разбор журнала теперь считает приход игрока сменой состава. **Сама проверка встроена в модуль (правило `join`):** словарь сохранений участников (`netmanager+0x1068`, достижения `значение+0x38`), состав матча и очередь прочитаны в живой памяти трёх игр и у всех одинаковы, поэтому каждый участник выносит вердикт сам — в перехвате штатной `process_pending_joins`, убирая недопущенных штатным `erase` (`0x513c80`). Проверено только тестами (`authority-check`: 12/12 CTest; 55/55 Python), **живьём НЕ проверено, основная сборка НЕ пересобрана** (игры открыты). Не сделано: стенд на троих с модулем (`peer=…@ID`), отличение входящего без модуля (он расколет матч), подсказка входящему в заголовке окна. **На момент записи открыты три тестовые игры (NET A — хост, NET B, NET C — входящий в лобби «Ready»): чтобы закончить пробу, хост должен пройти этаж и спуститься.**
>
> **21 сентября 2026, 08:00–08:30 — третья сессия пробы 0.1.3 (играл пользователь, семь этажей): всё из списка пробы работает по его слову, затем гость упал на Needle — причина найдена, исправлено, собрано, живьём НЕ проверено.** Подтверждено пользователем: потухший костёр, товар магазина, табло автомата пожертвований, какашки босса, помощь тестеру (F6/F7/F8). За 25 минут `faults 0`, `datagrams refused 0`. **Следование за этажом так и не проверено:** все шесть спусков обе игры сделали штатно сами (`floors followed 0`). **Падение:** модуль записал близнецу Needle (тип 881) состояние «прыжок» (6) со слов хоста, а игра на входе в это состояние сама кладёт первую точку в две очереди врага (`+0xc04`, `+0xc18`); у близнеца они были пусты, `deque::front()` бросил `std::out_of_range` внутри обновления врага — мимо защиты модуля (`faults` 0), игра гостя закрылась. Доказано по полному дампу упавшего процесса (снят `cdb -pv` с окна ошибки: `probe-0.1.3c\guest-crash.dmp`, стеки рядом). **Сделано два слоя:** (1) Needle готовится к прыжку теми же функциями игры (`0x99380`, `0x99190`); (2) у гостя `Entity_NPC::Update` (слот 3) идёт под защитой — враг, бросивший исключение, уходит `Remove` и создаётся заново со слов хоста, его вид до конца матча живёт своим поведением; счётчик `ENEMY UPDATES THROWN` в стенде сразу за `FAULTS`, в `.status` — «enemies whose update threw». Общий вывод: **состояние, записанное снаружи, пропускает код входа в него** — другие такие враги наверняка есть, теперь они дадут счётчик, а не падение, и каждый стоит разобрать как Needle. Попутно закрыты два долга стенда (детектор видит классы столкновений, слой и места подбираемого; замер разрыва копии пропускает выборки поперёк смены комнаты). Подробности — «Третья сессия пробы» в [j460-native-online-reuse.md](docs/j460-native-online-reuse.md). **Дальше: новая пара — комната с Needle (Womb/Utero; `needle leaps set up` растёт, `ENEMY UPDATES THROWN` 0), спуск хостом при госте в стороне от люка (`floors followed`), бой с призывами, матч на троих.** **Замер эффектов этой сессии потерян** (38 минут с боями): при штатном закрытии игра сначала обнуляет указатели, пробник упал с `InvalidState` вместо `OSError` и отчёт не записал; пробник исправлен (любая ошибка чтения переживается, отчёт пишется раз в минуту) — замер для следов на полу повторить в следующей паре.
>
> **21 сентября 2026, позже утром — вторая сессия пробы 0.1.3: ещё две находки исправлены и собраны (12/12 CTest, 54/54 Python), живьём НЕ проверены; три правки магазина тоже всё ещё НЕ проверены — пользователь до них не дошёл.** Сессия: `faults 0`, `datagrams refused 0`, игры закрыты штатно; помощь тестеру включилась только у NET A (клавиши пользователь отдельно не подтверждал); лишний враг гостя один раз ушёл через `Remove` без сбоя. **(1) Какашки босса у гостя не появлялись или оставались сломанными** — модуль пропускал занятую ячейку, а штатный `SpawnGridEntity` (`0x3ebca0`) стоящее удаляет и ставит новое; теперь рождённая ячейка ставится поверх декорации и поверх сломанной того же вида (запись `Born` несёт состояние). **(2) Хост ушёл на следующий этаж, гость остался, пара развалилась** — сделано следование за этажом: `floorEpoch` в теле и штатный `Game::StartStageTransition` (`0x2fdc10`, защёлка `Game+0x1ba78`); защёлка в покое и байты входа проверены в живых играх, **её значение во время перехода вживую не наблюдалось** (гость в люк так и не зашёл); следуется только обычный спуск. **(3) Следы на полу — пользователь просит, чтобы хост синхронизировал и их («в оригинале всё синхронно»): НЕ сделано.** Кровь — короткоживущие эффекты (`Entity_Effect`, таблица `0x763bdc`, слоты 1/3/10), впечатываемые в пол; нужны рождения с хоста событиями и блокировка своих у гостя. Два замера рождений боя не застали — данных о потоке нет; seed эффектов у сторон не совпадают, поле «кто породил» у эффектов пусто. Пробники сессии — `Binaries\native-test-results\probes\` (замер эффектов запускать ДО боя). Подробности — «Вторая сессия пробы» в [j460-native-online-reuse.md](docs/j460-native-online-reuse.md). **Дальше: новая пара (`Test-IsaacNative.py --play --cheats`) — магазин (костёр, товар, табло), бой с боссом, кладущим какашки, спуск на этаж хостом (гость должен пойти следом; смотреть `floors followed`), и замер эффектов во время боя.** Тестовых экземпляров нет.
>
> **21 сентября 2026, утро — первая живая проба 0.1.3 на локальной паре (играл пользователь): основа прошла, три находки в магазине исправлены и собраны, но живьём ЕЩЁ НЕ проверены.** За час игры `faults 0`, `datagrams refused 0`, потерь 0, правила не отключались; рукопожатие протокола 4 — `LIVE` сразу; дверь пройдена парой сама (разница 4–5 кадров). Находки пользователя и их причины (раздел «Первая живая проба 0.1.3» в [j460-native-online-reuse.md](docs/j460-native-online-reuse.md)): **(1) потухший костёр ранил гостя, монета под спрайтом** — посылка 0.1.2 неверна (`CollisionDamage` у потухшего костра и на хосте 1.0), игра меняет классы столкновений `+0x184`/`+0x188` → 0 и `RenderZOffset` `+0x354` → −1000; теперь они идут с хоста; **(2) автомат пожертвований считал только у хоста** — пожертвование совершает обновление автомата, когда доигрывает анимация монеты (счётчик сохранения №20, табло, достижения), у гостя она не доигрывала: **гость не получал ни счётчика, ни достижений**; по слову пользователя в штатной игре оно проигрывается у каждого — теперь автоматы 8 и 11 играет каждая игра сама по событию владельца; **(3) копия толкала товар магазина** — перехват отвечал за копию «столкнуться», а игра предмет с ценой не толкает никогда; теперь копия отвечает правилом игры (`0x2e8ae0`). Основная сборка пересобрана после закрытия игр: 12/12 CTest. **Дальше — новая пара и проверка этих трёх в магазине, затем бой с призывами и матч на троих.** Игры закрыты штатно, тестовых экземпляров нет. **Помощь тестеру:** `Test-IsaacNative.py --cheats` — только игре хоста (NET A) и только в изолированном экземпляре: без урона своему игроку (F6), урон по врагам ×20 (F7), F8 — убить врагов комнаты; собрано, живьём не проверено. Пока тестовые игры открыты, основную сборку пересобирать нельзя — компиляцию проверять в `Binaries\authority-check`.
>
> **21 сентября 2026 — ревью кода обмена и правки по нему: собрана 0.1.3 (протокол 4) — живьём НЕ проверена и НЕ выпущена.** Ревью `native_adapter.cpp` чтением, без запуска игр (раздел «Ревью кода обмена и правки 0.1.3» в [j460-native-online-reuse.md](docs/j460-native-online-reuse.md)). Исправлено без живой пробы: проверка диапазона в `ValidShot`, с коммита бомб сидевшая внутри комментария; таблицы комнаты гостя (`missing`/`orphans`/`aliases`), которые в долгом бою с призывами заполнялись и останавливали создание и удаление врагов; рукопожатие теперь сверяет `host` и `rules` из hello (ровно один хост, равные маски — иначе ожидание и отказ с причиной в заголовке); номер старта отправителя в каждом кадре (перезапуск одной стороны больше не означает вечное отбрасывание по `sequence`); **`faults` — теперь только сбои на потоке игры**, отброшенные датаграммы считаются отдельно (`datagrams refused`), третий сбой одного правила выключает его до конца матча; лишний враг гостя уходит `Remove`, а не `Kill` (слот 10 таблицы врага прочитан в EXE). **Чистая логика обмена вынесена в `native_state.hpp/.cpp` под тесты (`tests/native_tests.cpp`, CTest `native_exchange`) — 12/12 CTest, 54/54 Python.** Найдено и **не исправлено** (нужна живая проба или решение пользователя): идентичность игрока по контроллеру (двое на одном контроллере), только клавиатура (геймпад → модуль не стартует, *предположение по коду*), урон гостя по врагам не событие, правка кода игры не с потока игры, данные соседа без диапазонов, подбор без страховки по состоянию. **Первая проба 0.1.3 — локальная пара** (`Test-IsaacNative.py`: `FAULTS 0`, `datagrams refused` не растёт, бой с призывами — `npcSpawned` растёт после 48-го), затем матч на троих с уходом игрока. Всё, что ниже про 0.1.2 (костёр, `look`, `head_probe.py` для `+0x1338`), остаётся в силе и входит в 0.1.3.
>
> **20 сентября 2026, позже — 0.1.1 подтверждена в настоящем матче (`bodies applied` растёт, `faults 0`).** По новым находкам пользователя собрана **0.1.2 (протокол 3, правило `look`) — живьём НЕ проверена и НЕ выпущена**: урон от касания врага теперь от хоста (потухший костёр ранил гостя), копия игрока моргает при стрельбе (`HeadFrameDelay`, `Entity_Player+0x1338` — **смещение ещё не читано в живой памяти, сначала `head_probe.py`**) и проигрывает удар штатным уроном с `DAMAGE_FAKE`. **Открыто и важнее всего: у одного из друзей после матча в меню закрыты персонажи (в памяти осталось общее сохранение матча)** — у пользователя все сессии закрылись штатно, причина у друга не установлена, нужен его `log.txt`; до выяснения всем советуется копия сохранения перед игрой. Пользователь снял модуль у себя. Раздел «Вторая проба через Steam» в [j460-native-online-reuse.md](docs/j460-native-online-reuse.md).
>
> **20 сентября 2026, ночь — первая проба между Steam-клиентами (пользователь с друзьями, 0.1.0): обмен через Steam и рукопожатие заработали сразу (`LIVE`, оба соседа ответили, `faults 0`), перезапуск при уходе третьего игрока тоже.** Но копии шли штатным lockstep («лагает, как без authority»): **номера устройств у каждого процесса свои**, сосед слал контроллер 5, его копия у пользователя — 3, тела не доходили до копии (`bodies applied 0`; найдено чтением данных модуля в памяти, `inbox_scan.py`). Исправлено в **0.1.1 (протокол 2)**: получатель сам нумерует игрока отправителя по своему журналу. **Собрано, живьём не проверено** — ждёт следующего матча пользователя; обе стороны должны поставить 0.1.1. Раздел «Первая проба между Steam-клиентами» в [j460-native-online-reuse.md](docs/j460-native-online-reuse.md).
>
> **20 сентября 2026, поздний вечер — модули обмениваются сами, собран релиз для пробы между Steam-клиентами** (раздел «Свой обмен модулей…» в [j460-native-online-reuse.md](docs/j460-native-online-reuse.md)). Python-стенд из обмена убран: кадры `IAF1` (тело / выстрелы / мир, куски до 1184 байт, упаковка только занятых записей) идут по UDP (`listen=`/`peer=`; **проверено живьём, играл пользователь:** `FAULTS 0`, потерь 0, детектор за 9 минут без расхождений, «по плавности хорошо») или **через Steam** (`steam steampeer=ID64`: четыре плоских экспорта `ISteamNetworkingMessages` из `steam_api.dll` самой игры, канал 7460; экспорты в DLL игры проверены, **сам путь живьём НЕ проверен — нужны два аккаунта**). Пока сосед не услышан, модуль ничего в игре не меняет (`Live()`); мир и выстрелы шлются 30 раз в секунду, тело — 60; таблицы по контроллеру — до 64 (номера устройств под Steam растут от матча к матчу); хост под Steam — игрок с наименьшим номером устройства (*допущение по одному журналу*). **Запуска руками больше нет — установка** (`scripts/Install-IsaacAuthority.ps1`, `-Uninstall`): рядом с `isaac-ng.exe` кладутся `version.dll` (загрузчик: её импортирует EOS SDK игры) и `IsaacAuthorityNative.dll`; модуль сам читает журнал игры (начало и конец матча, своё устройство, Steam id соседей, уход игрока) и сам стартует и снимается. **Рукопожатие `HEL1`: пока модуль есть не у всех игроков матча, не меняется ничего, даже сверка контрольных сумм; через 60 с без ответа модуль сдаётся.** Проверено живьём на копии папки игры: загрузка игрой, авто-старт по журналу, `LIVE` у обоих, модуль только у одного → матч штатный и перехваты сняты, уход хоста → модуль гостя снялся и вернул сверку. Упаковка — `scripts/New-IsaacAuthorityRelease.ps1` → `Binaries\release\<имя>.zip` (`INSTALL.cmd`, `UNINSTALL.cmd`, русский `README.txt`). **Следующий шаг — первая проба с другом через Steam** (в заголовке окна должно появиться `Authority: ON`). Если Steam-путь не заговорит — первым делом проверить допущение о хосте и приём сессии (`AcceptSessionWithUser` в DLL есть, не используется). Журнал как источник состава матча — временно: найти эти данные в памяти игры. Тестовые экземпляры закрыты.
>
> **20 сентября 2026 — смена курса, предложенная пользователем** ([j460-native-online-reuse.md](docs/j460-native-online-reuse.md)): не строить рядом с игрой свои лобби, транспорт и создание игроков, а взять онлайн самой игры и заменить в нём только синхронизацию. Модель: свой персонаж (статы, предметы, статусы) авторитетен у владельца и ему доверяют, мир (положения, события) — у хоста; предсказание и сверка своего игрока тогда не нужны, задержка ввода исчезает. В бинарнике найдены фабрика сетевого сервиса RVA `0x629370` (1 — встроенный `ServiceLocalhost` на `WM_COPYDATA`, игрой не выбирается никогда), ворота кадра lockstep `0x510980`, приём ввода `0x502010`, автомат десинка `0x4fd900`. **Шаг 0 выполнен (модуль `IsaacAuthorityLocalhost.dll`, подключение `IsaacAuthorityAttach localhost <PID> <DLL>`): штатный онлайн-матч игры идёт между двумя изолированными экземплярами без Steam — лобби, обмен сохранениями, общий seed, оба игрока, lockstep 30 кадров/с, ввод каждого виден у обоих. Общее сохранение, собранное игрой, совпало с нашим `merge_shared` во всех 2096 элементах. Старт сначала зависал: игра клала своё сохранение под Steam id, а слоты лобби названы идентификаторами локального сервиса — модуль теперь даёт локальному пользователю эту личность. Задержка ввода штатного lockstep измерена: 134–149 мс против 15 мс в одиночной игре. Не работает только список Public Match (пять методов данных у `LobbyLocalhost` — заглушки); вход идёт через Quick Match. **Шаги 1 и 2 сделаны (модуль `IsaacAuthorityNative.dll`, стенд `scripts/Start-IsaacNativePair.ps1` + `scripts/Test-IsaacNative.py`): свой игрок отвечает клавиатуре за 19–42 мс вместо 134–149 мс штатного lockstep, сверка контрольных сумм кадров отключена и лобби не раскалывается, чужой игрок ставится по телу, присланному владельцем: его копия держится в пределах одного-двух шагов движения от оригинала, в покое совпадает, при плохой сети обмена отстаёт на время пути без телепортов. **Шаг 3 сделан: враги принадлежат хосту** — гость накладывает состояние врагов хоста поверх своих (по seed; рождённых в бою — по типу и близости), смерть врага приходит только от хоста, смертельный удар в симуляции гостя срезается. В бою положения врагов у сторон держатся в 0–2 пикселях вместо расхождения до 39, здоровье равно, комната зачищается у обоих одновременно. **Шаг 4 начат по пробе пользователя, сыгравшего в локальном матче сам (`Test-IsaacNative.py --play`): здоровье принадлежит владельцу** — сердца едут с телом, удар по чужой копии игнорируется, смерть копии приходит от владельца; пользователь подтвердил, что здоровье у сторон сошлось. **Дальше шаг 4 шёл по его же снимкам расхождений, по правилу, которое он сформулировал: «блокировать у других, синхронизировать с хостом».** Проверено живьём: поведение и анимации врагов с хоста, создание у гостя недостающего врага, зачистка комнаты по слову хоста, ранняя смерть копии, подбор как событие владельца (4 из 4), сетка комнаты (камни, какашки, TNT, паутина) с хоста. Собрано, но живьём не проверено: переход в комнату вслед за хостом, `Revive` копии, костры. Таблица правил, адреса и разбор одной моей ошибки (неверное смещение спрайта сломало мир гостя) — в [j460-native-online-reuse.md](docs/j460-native-online-reuse.md). **Снаряды врагов (мир хоста) и слёзы игроков (владельца) собраны во время перерыва пользователя и живьём НЕ проверены** — он видел, что у босса залп есть у одного и нет у другого, а эффекты слёз срабатывают по-разному; первая проба — сначала `shots_probe.py` (чтение полей в живой памяти), потом счётчики `projectiles made/ended/dropped`, `tears sent/made/ended/dropped`, `fire holds` и `FAULTS`. **Там же, без запуска игр, собрано и живьём НЕ проверено:** список подбираемого от хоста, общие монеты/бомбы/ключи от хоста, двери от хоста, ловушки-статуи в списке врагов, бомбы игроков от владельца, бомбы врагов от хоста, автоматы и попрошайки от хоста (касание гостя играет хост), страховка по этажу, факт урона красным сердцам (байт комнаты `+0x120c`), защита от неполных списков; **детектор расхождений** `scripts/Watch-IsaacNativeDivergence.py` (только чтение, запускать рядом с `--play`); каждое правило модуля отключается при запуске (`Test-IsaacNative.py --without … --restart`), так что на первой пробе сомнительное снимается без пересборки; пробники — `shots_probe.py`, `drops_probe.py`. **Смещения всех этих правил проверены чтением живой игры пользователя** (его штатный матч, только чтение): слёзы, снаряды, бомбы, подбираемое, счётчики, дверь, оружие; проверка поймала две ошибки в бомбах (флаги `+0x438`, а не `+0x428`; `+0x414` не писать). За живой пробой пары остаётся поведение: `Spawn` под чужим seed, `Morph`, обновление двери, удержание выстрела. **Вечером 20 сентября 2026 пользователь сыграл пару со всеми этими правилами сразу:** слёзы, снаряды, бомбы, список подбираемого, счётчики, двери, факт урона заработали без сбоев; найдено и исправлено падение гостя на составном боссе (части врага списками больше не создаются и не удаляются), добавлены питомцы владельца и ячейки сетки, рождающиеся в бою; детектор расхождений (`Watch-IsaacNativeDivergence.py`) назвал 38 расхождений — все про такие ячейки. Открыт вопрос лёгких заиканий кадра на одной машине: обмен модуля ни при чём (замерено), `OnlineInputDelay=5` в тестовых профилях — без доказанного эффекта, точный счётчик интервалов кадра добавлен в модуль и ждёт следующей сборки (исходник новее собранной DLL: `longFrames`, правило `lead`). Подробности — раздел «Первая живая проба пары» в [j460-native-online-reuse.md](docs/j460-native-online-reuse.md). **Что ещё придётся брать с хоста — разложено заранее в [j460-world-replication-map.md](docs/j460-world-replication-map.md)** (разбор REPENTOGON, чьи макеты для J273 совпали с J460, и 12 собственных дампов десинка штатного онлайна пользователя: 9 из 12 расколов — скорость игрока в третьем знаке, чего в нашей модели не бывает). Главный вывод: у игры есть свой формат комнаты (`EntitySaveState`, `GridEntityDesc`, `Room::save_entity`/`restore_entity`) и целого мира (`GameState`, с режимом «вход в идущий онлайн-матч») — комнату «в покое» брать им, а не разбирать каждый класс. Порядок оттуда: живая проба собранного → описатель комнаты хоста (подбираемое, автоматы, двери и вся сетка разом) → события владельца над миром (ключ/бомба/монета, касание автомата, факт урона красным сердцам, бомба) → лазеры врагов и ловушки → `GameState` целиком → свой детектор расхождений. Затем ворота кадра lockstep и перенос обмена в соединение самой игры.** Тестовые экземпляры закрыты (пользователь попросил перерыв без запуска игр). Список ниже — запасной план прежнего пути.

**Сеть проверена; дальше — вывести модель из пустой комнаты.** Порядок по убыванию пользы:

1. **Доделать ввод по итогам сетевых прогонов** (небольшие, без новых функций игры): глубина буфера воспроизведения должна подстраиваться под дрожание (сейчас четыре шага: мало для 80 мс, лишняя задержка для чистой сети); повтор последних команд в каждом пакете закрыл бы потери без повторов-догадок; зону нечувствительности 10 пересмотреть — при пошаговом вводе без потерь расхождение ровно нулевое.
2. **Переход комнат и бой с двумя игроками и предсказанием.** Модули `Coop`/`Predict` собраны с `ROOMS` и `NPCS`, но проверены только в стартовой комнате. Здесь же всплывёт главное ограничение предсказания: штатное обновление своего игрока имеет локальные последствия (двери, подбор, толчки врагов).
3. **Лобби как у штатного онлайна.** Сохранение лобби уже собирается (`scripts/isaac_save.py merge`, правило прочитано из сборщика игры RVA `0x51a450`), и с него должен запускаться **каждый** участник, хост тоже: хост — изолированный экземпляр, а не основная игра. Запуск одним шагом написан — `scripts/Start-IsaacLobby.ps1 -Saves <хост>,<гость>` — и без запуска игр (`-MergeOnly`) проверен на частичном пересечении: сохранение пользователя и копия сохранения друга дают 86 общих достижений из 90 и 223. **Живая проверка пройдена:** два экземпляра с одного файла приняли его, при одном seed 0 расходящихся комнат из 15 и то же проклятие, и на этой паре без изменений прошёл полный конвейер `Test-IsaacCoop.py --stepped` (449/449 команд, ошибка предсказания 0). Открытым остаётся побайтное сравнение нашего объединения с общим сохранением игры — нужна копия сохранения друга, снятая прямо перед сессией вдвоём. **Лобби теперь процесс, а не сценарий** ([j460-lobby-process.md](docs/j460-lobby-process.md), `scripts/isaac_lobby.py`): участники сообщают упакованное сохранение, хост при старте замораживает состав и раздаёт билеты, каждый кладёт общее сохранение поверх своего, готовность доказывается памятью игры (seed, сложность, отпечаток уровня, отпечаток флагов живого сохранения), опоздавшим отказ, а `Test-IsaacCoop.py --lobby` подключает только допущенные игры. Проверено тестами и настоящими процессами на loopback с запущенной парой (`go`, отказ опоздавшему, `abort` при чужом сохранении). **Осталось:** живой проход целиком на свежих экземплярах (`--launch`, меню, `go`, конвейер); затем наложение общего сохранения в работающую игру штатным десериализатором RVA `0x51adf0` (вызов внутрь игры) и сессия билета как сессия модулей — вместе с нативным транспортом.
4. **Вход клиента посреди этажа — сверх паритета:** в штатном онлайне его нет (слова пользователя), поэтому не первоочередной. [Вход в забег](docs/j460-run-join.md) уже работает переносом файлов (`Start-IsaacReplica.ps1 -PersistentGameData … -GameState …`, затем «Continue»), seed вручную не вводится. Но хост отдаёт состояние только в точках записи: старт забега, новый этаж, выход в меню. Следующий шаг — запись по требованию штатной функцией RVA `0x558ed0` (без аргументов, сама проверяет условия; вызывает `Game::SaveState` `0x2f9000` для `Manager+0xfa4`). **Это первый вызов внутрь игры на этом пути: пробовать на изолированном экземпляре, на потоке игры, со сверкой пролога.** Загрузка в работающую игру (`Game::RestoreState` RVA `0x2bb790`) — после.
5. **Присоединение без второго устройства ввода** через `PlayerManager::SpawnCoPlayer2` (RVA `0x5b9cd0`, найден по сигнатуре REPENTOGON, **не вызывался**): нужно выяснить, как новому игроку назначается контроллер, и найти безопасную точку вызова на потоке игры.
6. **Нативный транспорт** вместо Python-теста; метки времени пакетов сейчас сравниваются с часами получателя, на двух машинах это не работает и номер «своего» игрока от транспорта при присоединении (сейчас зашит контроллер 1, второй в списке).

Прежде чем трогать игру: заново определить процессы (`Get-Process isaac-ng`, окна «Isaac Authority - HOST» / «REPLICA») и выполнить `--check`. **Имена окон — не роли:** в замкнутой петле хостом был процесс окна «REPLICA» (не встаёт на паузу без фокуса), клиентом — окно «HOST» (ему нужны фокус и клавиатура); роль задают `--source-pid` / `--replica-pid`.

```powershell
.\scripts\Build-IsaacAuthority.ps1
python -m unittest discover -s scripts/tests -p test_*.py
python scripts/Test-IsaacRoom.py --source-pid <HOST_PID> --replica-pid <REPLICA_PID> --restart-runs --check
```

Последний результат — **12/12 CTest и 54/54 Python-теста** (21 сентября 2026; двенадцатый — `native_exchange`, тесты логики обмена выпускаемого модуля). Не запускать пройденные живые тесты только ради повторного подтверждения.

### Что пройдено в последних двух сессиях

| Этап | Итог | Отчёт |
| --- | --- | --- |
| **Сохранение лобби** | Штатный онлайн раздаёт не чьё-то сохранение, а пересечение прогресса участников (поправка пользователя). Разобран формат сохранения J460 и его нестандартная контрольная сумма; разбор и сборка побайтно точны на 100 настоящих файлах. Правило `isaac_save.py merge` **прочитано из кода игры** (сборщик RVA `0x51a450`): флаги — побитовое И, счётчики — беззнаковый минимум, счётчики заставок — знаковый, карты 1–2 бестиария — минимум по ключам, общим для всех; настройки, особые seed и карты 3–4 в общее сохранение не входят. Сверено с дампами 46 настоящих сессий: 72 уцелевших дампа общего сохранения разобраны до байта, необъяснённых расхождений нет. По копии сохранения друга: 17 общих сохранений укладываются и в неё, и в сохранение пользователя; побайтного равенства пока нет — нужна копия, снятая прямо перед сессией вдвоём (`isaac_save.py compare`). Файл, записанный первой версией инструмента из полного и чистого сохранений, игра приняла и дала по нему уровень чистого сохранения: 0 отличий из 15 комнат. | [j460-lobby-save.md](docs/j460-lobby-save.md) |
| **Процесс лобби** | Общая сессия создаётся как в оригинале: упакованные сохранения участников → общее сохранение при старте → билеты → общее поверх своего → доказательство готовности из памяти игры → `go`/`abort`; после старта вход закрыт, транспорт подключает только допущенные игры. 10 тестов и прогоны настоящих процессов на loopback с запущенной парой. | [j460-lobby-process.md](docs/j460-lobby-process.md) |
| **Вход в забег хоста** | Состояние забега переносится двумя файлами игры: экземпляр с копиями сохранения и `gamestate1.dat` через «Continue» оказывается в том же забеге — seed, этаж, проклятие, все 15 комнат, счётчики игрока и потраченная бомба совпали, игровое время перенеслось. Вошедший экземпляр отработал клиентом в полном конвейере: 449/449 команд, расхождение 0 на 406 сверках. Первый прогон не прошёл по критерию теста (телепорт первого выравнивания с 257 единиц) — критерий уточнён. Найдена штатная запись состояния по требованию, RVA `0x558ed0`; не вызывалась. | [j460-run-join.md](docs/j460-run-join.md) |
| **Общее сохранение** | Причина разных уровней при одном seed — сохранение. Три экземпляра, один seed, hard: копия сохранения хоста даёт 0 отличающихся комнат из 15 и то же проклятие, чистое сохранение — 5 отличающихся и без проклятия. Живой переход с коррекцией врагов прошёл через правую дверь в комнату 85, раньше недоступную: 7 из 7 врагов во всех 208 кадрах. Статически разобран штатный механизм (`PersistentGameData` в `Manager+0x14`, «только чтение» `+0x15`, старт матча `0x50e2a0`). | [j460-shared-save.md](docs/j460-shared-save.md) |
| **Плохая сеть и пошаговый ввод, 0.11** | Транспорт теста задерживает, переставляет и теряет пакеты. На сети 100 мс / 80 мс / 15 % прежний ввод дал 10 ложных сдвигов до 20,6 единицы с рывками назад: хост читал самую свежую команду и тратил на одну команду несколько шагов. Пошаговый ввод (одна команда на шаг с обеих сторон, буфер воспроизведения на хосте) — 0 сетевых сдвигов, расхождение 0,000 в одном прогоне и до 8,75 в другом (буфер 12 раз опустел), в конце 0,0 и 0,01. Свой игрок реагирует за 31 мс при 375 мс до слова хоста. Один прогон не прошёл и вскрыл ошибку тест-скрипта (ложный `passed`), исправлена. | [j460-impaired-network-test.md](docs/j460-impaired-network-test.md) |
| **Предсказание, 0.10** | Хост подтверждает в снимке номер потреблённой команды клиента (448 снимков из 450); клиент ведёт своего игрока сразу той же командой (только движение), реплика его не удерживает и сверяет с прошлым клиента. Расхождение после одной и той же команды: медиана 0,009, максимум 4,42 (один шаг — разница фаз двух игр); 44 мягкие доводки в покое, 0 сдвигов, 0 телепортов; в конце 0,0 и 0,008. С первой попытки. | [j460-prediction-test.md](docs/j460-prediction-test.md) |
| **Замкнутая петля, 0.9** | Клавиши только в окне клиента → модуль клиента опрашивает свою клавиатуру (449 отсчётов) → 448/448 команд INP1 → хост **без фокуса окна** симулирует второго игрока (+175 / −122 / −175, 2 слезы), его собственный игрок стоит → 450/450 снимков на клиенте побитово. 16 мс до хоста, 31 мс до клиента. С первой попытки. | [j460-closed-loop-test.md](docs/j460-closed-loop-test.md) |
| **Два игрока в снимке, 0.8** | Хост публикует всех игроков с номером контроллера (WLD1 версии 3), первого ведёт клавиатура HOST, второго — 480 команд INP1; реплика удерживает обоих. 450/450 снимков побитово, слёзы обоих игроков, в конце расстояние между играми 0,0 и 0,0; порядок обновления игроков совпал с порядком списка. | [j460-coop-players-test.md](docs/j460-coop-players-test.md) |
| Переход комнат, 0.5 | HOST проходит в дверь 84 → 97. `door`: реплику проносит её собственная дверь (подавление обновления игрока двери не отключает), модуль принимает новое место. `request`: снимки у двери потеряны, реплика сама вызывает `Game::StartRoomTransition`, 328 мс. Состояния побитово равны хосту по обе стороны двери. | [j460-room-transition.md](docs/j460-room-transition.md) |
| Коррекция врагов, 0.6 | Содержимое комнаты детерминировано её spawnSeed: реплика не создаёт копий, а сопоставляет своих врагов с врагами хоста по (тип, вариант, подтип, InitSeed) и после их обновления записывает состояние хоста. 5/5 во всех кадрах, в конце расстояние между играми 0,0. | [j460-enemy-correction-test.md](docs/j460-enemy-correction-test.md), [наблюдение](docs/j460-enemies-research.md) |
| Урон и смерть, 0.6 | HOST сам убил червя: здоровье 10 → 6,5 → 3 пришло на реплику в тех же снимках, враг убит штатным `Entity::Kill()` в снимке, где хост перестал его перечислять. | там же |
| Ввод клиента, 0.7 | Один слот менеджера ввода (`WithDevice`, слот 29); команды INP1 по UDP двигают и заставляют стрелять настоящего игрока через физику игры, молчащий клиент = нейтраль. | [j460-client-input-test.md](docs/j460-client-input-test.md) |
| Второй игрок, 0.7 | Виртуальный контроллер 1 нажал «присоединиться» и «подтвердить» — игра сама создала второго игрока (локальный кооператив, splitscreen). Он прошёл весь сценарий по сети, первый игрок сдвинулся на 0,0. | там же |

### Что важно помнить

- **Уровень задают seed, сложность и сохранение.** При одном seed и одной сложности чистое сохранение даёт 5 других комнат из 15 и не даёт проклятия, копия сохранения хоста — тот же уровень целиком и то же Curse of Darkness ([проверено на трёх экземплярах](docs/j460-shared-save.md)). Моды ни при чём: все отключены и комнат не добавляют. Новый экземпляр запускать только так: `Start-IsaacReplica.ps1 -PersistentGameData <сохранение хоста>`; файл хоста только читается (у основной игры он в Steam Cloud: `Steam\userdata\<id>\250900\remote\rep+persistentgamedata1.dat`). `--check` теперь считает разные проклятия и сложность проблемой. Реплика по-прежнему нативно сверяет каждую комнату назначения.
- **Состояние забега — файл `gamestate1.dat`** (за пунктом «Continue»): пишется при старте забега, смене этажа и «Exit game», грузится только рядом со своим сохранением. С копиями обоих файлов новый экземпляр входит в тот же забег; место игрока файлом не задаётся («Continue» ставит его у двери, в двух играх у разной) — его задаёт снимок хоста. Присоединение второго игрока в продолженном забеге разрешено.
- **Для прогонов конвейера основная игра больше не нужна**: два изолированных экземпляра не встают на паузу без фокуса; клавиатуру получает тот, чьё окно в фокусе.
- **Клиент, вошедший из другого места, в первом снимке телепортируется сверкой** на место хоста — это правильно; тест допускает телепорт только как первое выравнивание.
- **Набор символов может попасть на анимацию экрана**: при вводе seed в пятом экземпляре дошли три символа из восьми — это показал снимок до подтверждения. Снимок после *каждого* шага меню обязателен и для уже проверенной последовательности.
- **Ввод seed в новом экземпляре** (нужен, только если нет файла состояния) — помощником с защитой фокуса и снимком **окна игры** (не всего экрана) после каждого шага: Enter, Enter, Space (слот 1; «Delete file» не выбирать), Enter (New Run; Online не трогать), Tab, восемь символов, Enter, Enter. Сложность на экране персонажа берётся из сохранения.
- **Присоединение второго игрока опрашивается только у существующих устройств ввода.** Сработало благодаря подключённому геймпаду (контроллер 1) — это DualSense по Bluetooth, он не XInput, `XInputGetState` его не видит; проверять через `Get-PnpDevice` («HID-compliant game controller»). Без второго устройства нужен другой способ; кандидаты — `PlayerManager::SpawnCoPlayer2` (RVA `0x5b9cd0`, по сигнатуре REPENTOGON) и функция RVA `0x3a6450` из скрытой комбинации, оба не проверены.
- **[REPENTOGON](docs/repentogon-reference.md) — справочник, не зависимость.** Он для билда J273 и понижает игру до него. Базовый `Entity` и нижняя часть `Game` совпадают с J460 один в один, но `Entity_Player` сдвинут (`ControllerIndex` у них `0x160c`, у нас `0x1618`), верх `Game` тоже; из 13 проверенных сигнатур в J460 однозначно нашлись 7. Любое смещение и адрес оттуда проверять на J460 перед использованием.
- **Игра без фокуса окна продолжает опрашивать контроллер второго игрока и симулировать его** (проверено петлёй): хосту не нужен ни фокус, ни человек. Клавиатура же достаётся только окну с фокусом, а основная игра пользователя (`PauseOnFocusLost=1`) без фокуса стоит — поэтому клиентом в петле может быть только она.
- **На клиенте нельзя подслушивать ввод — надо опрашивать.** Реплика подавляет обновление игроков, а про движение и стрельбу игра спрашивает именно в нём. Модуль клиента сам вызывает оригинальную `WithDevice` для контроллера 0 раз в 8 мс; `deviceId` может быть нулём, `out` — нет.
- **Временная база сверки — номер шага, а не «последняя команда».** Игра читает значение каждого действия осей ровно раз за обновление игрока, поэтому чтение «влево» — граница шага. Хост обязан играть ровно одну команду на шаг (буфер воспроизведения `input::Playout`), клиент — снимать одну команду на шаг своего игрока. Иначе при дрожании сети сверка исправляет ошибки, которых нет, и дёргает игрока туда-обратно. Для предсказания использовать модули `steppedHost`/`steppedClient` (`Test-IsaacCoop.py --stepped`), а не прежние.
- **`passed` в summary выставлять один раз, после последней проверки.** В `Test-IsaacCoop.py` флаг ставился в середине, и отказавшая поздняя проверка оставила `passed: true` при непустом `failures` (прогон `predict-20260919-235434-554809`, сохранён как улика, в манифест не внесён). Исправлено; в остальных тест-скриптах флаг и так последний.
- **Две игры никогда не шагают в фазе** (верно для прежних модулей ввода на чистой сети; см. пункт выше). Игрок обновляется 60 раз в секунду, команды снимаются 64 раза: «состояние после команды N» в двух играх честно отличается на один шаг ходьбы (4,4 единицы). Поэтому зона нечувствительности сверки — 10, а остаток закрывается доводкой, только когда оба стоят. Не трактовать расхождение в один шаг как ошибку физики.
- **Модули одного процесса находят друг друга по экспорту**, а не по имени файла (имена несут хеш): модуль мира перебирает загруженные модули и ищет `IsaacAuthorityInputAcknowledged`; повторный поиск не чаще раза в 0,5 с, поэтому модуль ввода, подключённый после модуля мира, виден с задержкой до полусекунды.
- **Второй игрок появляется в двух играх в разных местах** (в прогоне предсказания — 5,2 единицы разницы): удерживаемого игрока это не касается, предсказанного доводит сверка.
- **Предсказывается только движение.** Клиент локально подставляет своему игроку последнюю снятую команду без стрельбы и кнопок: иначе слёзы задваиваются (локальные и копии с хоста). Настоящее устройство на номере контроллера своего игрока (геймпад) им не управляет.
- **Контроллер клиента назначает транспорт, не клиент:** модуль клиента пишет в команду «контроллер 0» (своя клавиатура), `isaac_input.route` подставляет сессию хоста и номер контроллера, выданный хостом.
- **Идентичность игрока — место в списке**, а не контроллер: два игрока могут делить контроллер (Jacob и Esau). Игра обновляет игроков в порядке списка: источник снимает мир после последнего, реплика применяет с первым.
- **После рестарта забега игроки стоят у нижней стены** первой комнаты (y = 380; стена на 410): сценарии движения вниз упираются в неё.
- **Одноимённый модуль из другой папки инжектор не загрузит.** Пакет `Coop` содержит копию модуля ввода под именем пакета (`IsaacAuthorityCoopInput-<хеш>.dll`), потому что `IsaacAuthorityInput-<хеш>.dll` уже загружен в HOST из пакета `Input`.
- **Слот публикации мира вырос до 3168 байт.** `isaac_world.read_slot` читает размер из дескриптора модуля и принимает и прежние 3072: скрипты репозитория совместимы и с установленным пакетом `Npc`.
- **Что известно о штатном онлайне со слов пользователя (из первых рук, не из бинарника):** войти посреди забега нельзя; ограничения открытого подстраиваются под всех игроков лобби — не открытое хотя бы у одного не появляется ни у кого, так же уровень магазина и пожертвования. Мой вывод «рассылается сохранение одного участника» был неверен и зачёркнут в документах. Догадки по строкам журнала помечать как догадки и при первой возможности сверять с пользователем.
- **Сохранение (`persistentgamedata<слот>.dat`) читается и пишется `scripts/isaac_save.py`.** Контрольная сумма — CRC-32 с *собственной таблицей игры* (семь арифметических сдвигов из восьми), затравка `0xFEDCBA76`, по байтам от 16-го до самой суммы; стандартный CRC-32 и вариант из редакторов сохранений не подходят. Объединение повторяет сборщик общего сохранения игры (RVA `0x51a450`): И по флагам, минимум по счётчикам (заставки — знаковый), пересечение ключей карт 1–2 бестиария; настройки, особые seed, карты 3–4 — хоста (в игре они у каждого свои и на симуляцию не влияют). Файлы пользователя только читать, готовый файл не перезаписывать.
- **Дампам `online_logs/sessions/` нельзя верить по именам.** `persistentgamedata1_begin.dat` — не сохранение до сессии, а живые данные (общий контекст) в момент обратной записи; настоящее сохранение — `_end`. `sharedsave_*.dat` игра пишет из уже освобождённой памяти: 20 из 92 — чужая память, у уцелевших первые 4 байта (достижения 0–31) бывают затёрты указателем кучи. Сессия с двумя стартами подряд собирает второе общее сохранение из живых данных первого забега. Подробности и обратная запись (RVA `0x519980`) — в [j460-lobby-save.md](docs/j460-lobby-save.md).
- **Штатный онлайн — lockstep с задержкой ввода, и наших задач сверки в нём нет; заимствовать стоит другое** ([разбор по строкам EXE](docs/j460-online-path-research.md)): **общее сохранение** (`Shared save user ID`, `PersistentGameData ReadOnly`) — вероятное решение нашей проблемы «один seed, разные комнаты»; выдачу номера устройства от лобби; передачу полного состояния для входа и восстановления (`SYNC_PLAN_SYNC`, кандидаты `Game::SaveState`/`RestoreState`); повтор последних команд в каждом пакете ввода. Ничего из этого не декомпилировалось.
- **Путь онлайна для стенда не подходит** ([заметки](docs/j460-online-path-research.md)): онлайн только через Steam, безопасный пункт — Friend Match, два экземпляра одного аккаунта не сыграют. `--networktest` меню не меняет. Ключи `--set-stage=`, `--set-stage-type=`, `--load-room=` существуют, не проверялись.
- **Коррекции врагов может не хватить для реальной сети:** проверено на loopback с 60 снимками в секунду; локальный ИИ за шаг в 2–5 % случаев выбирает другое состояние. Запасные пути — подавление обновления с передачей анимации или передача RNG сущности.
- **Не сделано в мире:** враги и снаряды, появившиеся после загрузки комнаты; смерть врага при позднем подключении; здоровье игрока, предметы, награды; обратный переход, большие комнаты, телепорты. Ветки `settled` и `vanished` перехода в живых прогонах не срабатывали.

### Состояние игр и правила живых прогонов

На момент передачи открыты **две** игры — лобби `17e59d92`, запущенное `Start-IsaacLobby.ps1` с объединения сохранения пользователя и копии сохранения друга: окна «Isaac Authority - LOBBY 17e59d92 #1» (PID 49620) и «#2» (PID 45432), оба изолированные экземпляры в стартовой комнате забега с **введённым** seed `CZH4 8E6W`, hard, по одному игроку, перехваты отключены, уровни совпадают целиком. В последнем прогоне хостом был №2 (без фокуса), клиентом — №1. Основная игра пользователя не запущена; прежних экземпляров (HOST, REPLICA, SHARED SAVE, JOINED) к началу этого прогона уже не было — закрыты не мной. PID после перезапуска меняются — определять заново.

- Живые тесты берут фокус окна HOST и нажимают клавиши (около 12 секунд, с боем — до минуты): предупреждать пользователя. `Test-IsaacInput.py` клавиш игры не нажимает, только R при `--restart-run`. `Test-IsaacCoop.py` берёт фокус REPLICA (присоединение), затем HOST, всего около 30 секунд.
- **Windows может не отдать фокус**, если пользователь в этот момент работает в другом окне: тест падает с «HOST lost focus» до первого нажатия, ничего не подключив. Это не сбой модулей; повторить, когда пользователь не печатает, и не перехватывать окно раз за разом.
- **Состояние окон — главный источник сбоев тестов, не модулей.** HOST (`PauseOnFocusLost=1`) при потере фокуса открывает меню паузы и держит его; признак — `Game+0x23a74`. REPLICA не останавливается никогда и в комнате с врагами погибает. `Game+0x1d520` — экран смерти, там Esc означает выход в главное меню: тест шлёт Esc только при открытом меню паузы и живом игроке, на экране смерти перезапускает клавишей Space. Тёмный экран HOST — проклятие темноты этого seed, не пауза; паузу определять по счётчику кадров `Game+0x264f8`.
- После прогона игры остаются в комнате с врагами — возвращать их `--restart-runs --check`.
- **Не нажимать клавиши вслепую:** снимок экрана после каждого шага в меню. В этой сессии слепые Enter запустили забег, и это было ошибочно приписано ключу `--networktest`.
- В процессах загружены и неактивны DLL этапов Pair, World, Room, Npc, Input и Coop нескольких сборок (в REPLICA от первой попытки остался ещё и `IsaacAuthorityInput-98ed72ca.dll` из папки `Coop`). Установщик `Install-IsaacRoom.ps1 -Stage Room|Npc|Input` кладёт DLL под именами с хешем содержимого, поэтому новая сборка загружается рядом без перезапуска игр. Проверка «модуль загружен» в старых установщиках из 64-битного PowerShell не видит модулей 32-битной игры.
- Пакет `IsaacAuthority/Room` заморожен на проверенной сборке со своими копиями скриптов: скрипты репозитория рассчитаны на слот публикации 3072 байта и с теми бинарниками несовместимы. Для мира использовать `IsaacAuthority/Npc` и `Test-IsaacRoom.py --stage npc`, для ввода — `IsaacAuthority/Input` и `Test-IsaacInput.py`, для двух игроков — `IsaacAuthority/Coop` и его `Test-IsaacCoop.py`.

### Рабочие точки входа

- `native/IsaacAuthority/src/world_adapter.cpp` — источник и реплика мира; флаги `ISAAC_WORLD_SOURCE`, `ISAAC_WORLD_ROOMS` (поиск комнаты, запрос перехода, фаза «в пути», принятие собственной смены комнаты), `ISAAC_WORLD_NPCS` (враги, коррекция после их обновления, смерть, защита игрока реплики), `ISAAC_WORLD_PLAYERS` (состав `Team`, ведущее обновление шага, удержание всех игроков, контроллер `+0x1618`), `ISAAC_WORLD_PREDICT` (подтверждение команды в снимке, поиск модуля ввода по экспорту, свой игрок без удержания: `FindOwn`, `RememberOwn`, `MendOwn`).
- `native/IsaacAuthority/src/world_state.cpp`, `world_receiver.cpp` — формат WLD1 версий 1–3 (слёзы, враги, игроки с подтверждением команды), ID, epoch, свежесть, приём UDP; чистые правила `SamePlace`, `Decide`, `Direction`, `Departed`, `Prediction::Reconcile`.
- `native/IsaacAuthority/src/input_adapter.cpp`, `input_state.cpp` — перехват менеджера ввода, протокол INP1, окно «только что нажато», счётчик опросов; с флагом `ISAAC_INPUT_CLIENT` — сторона клиента: опрос своего устройства (`Capture`, `Compose`), слот команды, нейтраль для локального контроллера 0; с `ISAAC_INPUT_PREDICT` — ещё и подстановка снятой команды своему игроку. С `ISAAC_INPUT_STEPPED` — пошаговый ввод: буфер воспроизведения `input::Playout` на хосте, съём команды на границе шага на клиенте. Общая подстановка — `Inject`; экспорт `IsaacAuthorityInputAcknowledged` отдаёт номер команды, движение из которой игра прочитала последней.
- `scripts/Test-IsaacRoom.py` (`--stage room|npc`, `--route door|request`, `--fight`, `--restart-runs`, `--check`), `isaac_level.py`, `isaac_world.py`; `scripts/Test-IsaacInput.py` (`--observe`, `--join`, `--controller`), `isaac_input.py`; `scripts/Test-IsaacCoop.py` (`--stepped`, `--predict`, `--loop`, `--delay-ms`, `--jitter-ms`, `--loss`, `--network-seed`, `--controller`, `--restart-runs`, `--check`; модель канала — `scripts/isaac_link.py`) — присоединение второго игрока в обеих играх и сценарий двух игроков; с `--loop` клавиши идут в окно клиента (`--replica-pid`), а транспорт несёт захваченные команды хосту (`--source-pid`).
- `scripts/Install-IsaacRoom.ps1 -Stage …`, `scripts/isaac_save.py` (`info`, `shared` — дамп общего сохранения игры, `merge` — сохранение лобби, `compare` — дамп игры против нашего объединения), `scripts/isaac_lobby.py host|join …` (процесс лобби; `session.json` для `Test-IsaacCoop.py --lobby`), `scripts/Start-IsaacLobby.ps1 -Saves <сохранения участников> [-MergeOnly]` (лобби одним шагом на одной машине), `scripts/Start-IsaacReplica.ps1 -PersistentGameData <сохранение лобби> -GameState <состояние забега> -WindowPosX … -WindowPosY … -GameArguments …`, `native/IsaacAuthority/src/isolated_launch.cpp`.
- `reverse/ghidra/ExportIsaacRoom.java` — `text:Фрагмент_строки_журнала` (все функции, использующие строку; `_` вместо пробела), `name:МетодLua` (адрес функции или смещение поля через регистрацию LuaBridge), `data:RVA`, `addr:RVA`, RVA. Результаты — `Binaries/research/j460-*/`.
- Профили адресов J460: `native/IsaacNetProbe/profiles/j460-world.json`, `j460-room.json`, `j460-npc.json`, `j460-input.json`, `j460-coop.json`, `j460-save.json` (сохранение, сложность, проклятия, старт штатного матча) (в нём же непроверенные адреса, найденные по сигнатурам REPENTOGON).

## Цель и принятое направление

Пользователь выбрал **замену сетевой модели Isaac на авторитетный хост**: хост считает мир, клиенты предсказывают движение и корректируются по его состоянию. Временное расхождение не должно разделять лобби, задержка клиента не должна останавливать хост. Исправление отдельных случаев старого lockstep не является основным направлением; сохранённые десинки нужны как проверочные случаи.

**Полноценного альтернативного мультиплеера в игре пока нет.** Между двумя настоящими J460 проверены реплика игрока, жизненный цикл обычных слёз, настоящий переход через дверь, коррекция врагов комнаты с уроном и смертью по решению хоста. Проверено, что хост симулирует игрока по сетевому вводу, что второй игрок присоединяется штатным локальным кооперативом, что снимок мира несёт обоих игроков на реплику, что петля замыкается — клавиатура клиента ведёт его игрока на хосте, а клиент видит результат — и что клиент может вести своего игрока сразу, а хост его поправляет. Всё это в одной пустой комнате; транспорт — тест на loopback без потерь, сверке ещё не с чем было расходиться; предметы, награды, здоровье игрока и новые сущности не синхронизированы.

## Сделано и проверено

| Компонент | Фактический результат |
| --- | --- |
| Совместимость патчера | Исходники 1.4.0 распознают семь областей патчей оригинальной J460. Изменение сигнатур для неё не потребовалось. |
| Диагностика | IsaacNetProbe записывает debug-output игры в JSONL. Работоспособность подтверждена реальными забегами и десинками. Сырой ввод и полное состояние мира не записывает. |
| Поиск установки | Steam registry + основные/дополнительные библиотеки, modern/legacy libraryfolders.vdf, manifest, ручной выбор при неоднозначности. 13 проверок прошли. Диск C не обязателен. |
| Сетевой стенд | `native/IsaacAuthority`: авторитетное движение, prediction/reconciliation, ограниченные очереди, отсечение старых/повторных пакетов, UDP localhost. Хост продолжает шаги при остановке клиента; после восстановления связи состояния сходятся. Физика стенда упрощённая. |
| Чтение J460 | Найдены указатели игроков, положение/скорость и кольцевая история кадров. Проверено внешнее чтение запущенного оригинального EXE без записи в память. Восемь тестов парсера/гонок/пакетов прошли. |
| Игра → сетевой наблюдатель | Реальные позиции/скорости двух игроков переданы в отдельный C++-процесс. 136 снимков приняты; конечные float32 совпали побитово. Это наблюдатель, не второй игровой клиент. |
| Эксперимент коррекции | **Живой одиночный тест J460 прошёл 19 сентября.** Наблюдение: 127 обновлений без коррекций. Второй тест: 244 обновления, ровно один сдвиг X на +8, следующее обновление сохранило новую позицию. Поток 12600, PID 40064; оба раза слот восстановлен, потерь записей нет. |
| Сетевое применение состояния | **Живой тест прошёл 19 сентября, 19:26 UTC.** `IsaacAuthorityNetwork.dll` принял 5 состояний от отдельного Python-хоста по UDP loopback, применил их на потоке 43720 в PID 21580, отклонил 8 пакетов. 96 обновлений, все float32 и следующие обновления проверены; позиция возвращена к исходной, слот восстановлен. Это тестовый отправитель, не второй Isaac. |
| Две реальные игры | **Живой тест прошёл 19 сентября, 19:57 UTC.** Хост PID 42680: 622 обновления. Реплика PID 12800: 619 обновлений, 271 применение с точным float32-соответствием исходному кадру, 605 собственных обновлений игрока пропущены. За секунду без пакетов хост выполнил 60 обновлений и двигался, реплика удерживала состояние; после восстановления передачи сошлась с хостом. Слоты обеих игр восстановлены. |
| Жизненный цикл слёз | **Живой тест прошёл 19 сентября, 20:38 UTC.** 194 применённых снимка, 3 созданных и 3 удалённых копии, остаток 0. Все поля совпали с источником как float32. За паузу передачи хост выполнил 60 обновлений, клиент удержал живые слёзы в 41 проверенном обновлении. Контрольный новый epoch удалил последнюю копию, 3 старых пакета отклонены. Реального перехода комнаты в этом тесте не было. |
| Переход комнат | **Живой тест прошёл 19 сентября, 21:32–21:34 UTC, по двум маршрутам.** HOST проходит в настоящую дверь 84 → 97. `door`: реплику проносит её собственная дверь, модуль продолжает в новой комнате (188 + 226 снимков). `request`: 11 снимков у двери задержаны, реплика сама вызывает `StartRoomTransition` и достигает комнаты за 328 мс (189 + 204 снимка). Все состояния совпали как float32, слёзы реплицированы по обе стороны двери, ошибок 0. Один переход через одну дверь; при одном seed и разных сохранениях различаются 5 комнат из 15. |
| Коррекция врагов | **Живой тест прошёл 19 сентября, 22:01–22:02 UTC, по двум маршрутам.** Реплика сопоставляет собственных врагов с врагами хоста по InitSeed и после их обновления записывает состояние хоста. 5/5 врагов во всех 208 и 204 кадрах, 1605 и 1530 коррекций, все состояния побитово равны хосту; в конце расстояние между играми 0,0. Один вид врага, одна комната, loopback; смерть и новые сущности не реализованы. |
| Ввод клиента на хосте | **Живой тест прошёл 19 сентября, 22:16 UTC.** 390 команд по UDP, 0 отклонено; настоящий игрок хоста двигался и стрелял по ним через штатную физику игры, тест не нажал ни одной клавиши; при молчании клиента игрок остановился. Затем (22:37 UTC) виртуальный контроллер 1 присоединился штатным локальным кооперативом, и второй игрок прошёл тот же сценарий по сети, а первый не сдвинулся. Loopback; сторона клиента не сделана. |
| Два игрока в снимке мира | **Живой тест прошёл 19 сентября, 23:04 UTC.** Обе игры создали второго игрока штатным присоединением (виртуальный контроллер 1). Хост опубликовал 450 снимков с двумя игроками (контроллеры 0 и 1), реплика применила все 450, состояния побитово равны хосту; первого игрока вела клавиатура HOST, второго — 480 команд INP1 (принято 480). Слёзы обоих игроков (2 + 2) созданы и удалены, 899 обновлений игроков реплики подавлено, в конце расстояние между играми 0,0 и 0,0, слоты игрока, слезы и ввода восстановлены. Стартовая комната, без двери и врагов, loopback. |
| Замкнутая петля | **Живой тест прошёл 19 сентября, 23:18 UTC, с первой попытки.** Хост — процесс без фокуса окна, клиент — игра с фокусом; клавиши нажимались только в окне клиента. Модуль клиента снял 449 отсчётов клавиатуры (149 с нажатыми клавишами), хост принял 448 команд из 448 и сам симулировал второго игрока; клиент применил 450 снимков из 450, побитово. Собственный игрок хоста не сдвинулся, слёзы только у игрока клиента (2), в конце расстояние между играми 0,0 и 0,0. От клавиши до движения на хосте 16 мс, до клиента 31 мс. Без предсказания, loopback, транспорт — тест. |
| Предсказание своего игрока | **Живой тест прошёл 19 сентября, 23:32 UTC, с первой попытки.** Хост подтвердил потреблённую команду в 448 снимках из 450; реплика не удерживала своего игрока (подавлено 451 обновление вместо 901) и сверила его 450 раз, 421 — с записью собственной истории. Расхождение после одной и той же команды: медиана 0,009, среднее 0,76, максимум 4,42 — один шаг ходьбы; при ходьбе 144 сверки из 218 дали ровно ноль. 44 доводки в покое, 0 сдвигов, 0 телепортов; в конце 0,0 и 0,008. Свой игрок двинулся на экране клиента через 31 мс после клавиши, слово хоста пришло через 47 мс. Сдвиг и телепорт вживую не срабатывали. |
| Предсказание через плохую сеть, пошаговый ввод | **Четыре живых прогона 19 сентября, 23:45–23:57 UTC; три прошли, один нет.** Сеть в тесте: задержка, дрожание и потери в обе стороны. Прежний ввод на 80 мс / 30 мс / 5 %: расхождение до 8,77, сетевых сдвигов 0. На 100 мс / 80 мс / 15 %: расхождение до 20,6, 10 сетевых сдвигов, игрока дёргает назад — хост тратил на одну команду несколько шагов. Пошаговый ввод на той же сети: принято 374 команды из 374 вместо 236, сетевых сдвигов 0; в первом прогоне расхождение 0,000 на всех 182 сверках, но подтверждение один раз пошло назад — прогон не прошёл; после правки — расхождение до 8,75 (буфер опустел 12 раз), 33 доводки, в конце 0,0 и 0,01. Одна машина, пустая комната. |

Последняя сборка Authority: MSVC x86 Release, `/W4 /WX`, **11/11 CTest и 32/32 Python-теста** прошли 20 сентября. Подробности: `Binaries/authority-build/Testing/Temporary/LastTest.log`. Последняя живая проверка: [предсказание через плохую сеть](docs/j460-impaired-network-test.md). Предыдущие этапы: [предсказание своего игрока](docs/j460-prediction-test.md), [замкнутая петля](docs/j460-closed-loop-test.md), [два игрока в снимке мира](docs/j460-coop-players-test.md), [ввод клиента на хосте](docs/j460-client-input-test.md), [коррекция врагов](docs/j460-enemy-correction-test.md), [настоящий переход комнат](docs/j460-room-transition.md), [жизненный цикл слёз](docs/j460-world-tears-test.md), [реплика игрока в двух играх](docs/j460-game-pair-test.md).

## Текущая установка и артефакты

- Игра: `C:\Program Files (x86)\Steam\steamapps\common\The Binding of Isaac Rebirth`.
- Проверяемая версия: Repentance+ `v1.9.7.17.J460`, Steam build `22878971`, патч 21 апреля 2026.
- EXE оригинальный, SHA256 `3bdfc8bae0dc7e334b76009d0ad45dfbb16ee5f00c06ffbc3a0094e34d44616b`. Патчи разрешения модов/аналитики сейчас не применены.
- Установлен пакет диагностики **0.2.1** в подпапке игры `IsaacNetProbe`. Запись включается ярлыком **Isaac Diagnostics**; обычный запуск через Steam сам по себе её не включает.
- Пакет другу: `Binaries/IsaacNetProbe-0.2.1-win-x86.zip`. Не путать с `Binaries/IsaacOnlineModded-1.4.0-win-x64.zip` — это отдельный старый по назначению патчер модов.
- Native-журналы: `%LOCALAPPDATA%\IsaacNetProbe\logs`; журналы игры: `Documents\My Games\Binding of Isaac Repentance+\online_logs`.
- Authority 0.1.0-experimental установлен в подпапку игры `IsaacAuthority`: DLL, attach EXE, тестовый скрипт и `installation.json`. Установленный скрипт берёт бинарники рядом с собой и копирует проверенные журналы в `%LOCALAPPDATA%\IsaacAuthority\verified-reports`. После теста хук отключён; DLL остаётся загруженной до выхода из игры. Автозапуска нет. Исходные бинарники: `Binaries/authority-build/Release/`.
- Сетевой эксперимент 0.2.0-experimental установлен отдельно в `IsaacAuthority/Network`; установщик — `scripts/Install-IsaacAuthorityNetwork.ps1`. Автозапуска нет. Живая проверка новой DLL прошла; `installation.json` содержит результат и SHA256 summary. Для обновления загруженной DLL требуется перезапуск игры.
- Пара игровых модулей 0.3.0-experimental установлена в `IsaacAuthority/Pair`: Source DLL, Replica DLL, attach EXE и проверочный скрипт. Живой тест прошёл, хеш summary сохранён в manifest. Установщик — `scripts/Install-IsaacGamePair.ps1`.
- Модули обычных слёз 0.4.0-experimental установлены в `IsaacAuthority/World`: WorldSource/WorldReplica DLL, attach EXE, скрипты и codec. Установщик — `scripts/Install-IsaacWorld.ps1`, тест — `scripts/Test-IsaacWorld.py`. Manifest содержит результат живого теста; оба перехвата игрока/слезы после проверки восстановлены, созданные копии удалены.
- Модули перехода комнат 0.5.0-experimental установлены в `IsaacAuthority/Room`: RoomSource/RoomReplica DLL, attach EXE, `Test-IsaacRoom.py`, `isaac_level.py` и зависимости. Установщик — `scripts/Install-IsaacRoom.ps1`; DLL устанавливаются под именами с хешем содержимого, имена — в `installation.json`. Manifest содержит оба пройденных живых прогона с SHA256 их summary.
- Модули врагов 0.6.0-experimental установлены в `IsaacAuthority/Npc` тем же установщиком (`-Stage Npc`); manifest содержит пройденный прогон боя (последняя сборка).
- Модуль ввода 0.7.0-experimental установлен в `IsaacAuthority/Input` (`-Stage Input`), тест — `scripts/Test-IsaacInput.py`; manifest содержит пройденный прогон.
- Модули двух игроков, петли, предсказания и пошагового ввода 0.11.0-experimental установлены в `IsaacAuthority/Coop` (`-Stage Coop`): CoopSource/CoopReplica, PredictSource/PredictReplica, модуль ввода хоста, модуль ввода клиента, его предсказывающий вариант и пара пошаговых модулей `SteppedHost`/`SteppedClient` (все под именем пакета), `isaac_link.py`, attach EXE, `Test-IsaacCoop.py` и зависимости. Manifest содержит шесть пройденных прогонов (два игрока, петля, предсказание, два прогона прежнего ввода через плохую сеть, пошаговый ввод через жёсткую сеть) с SHA256 их summary; отчёты всех попыток — в `Binaries/diagnostics/coop/`. **Непредсказывающие модули пересобраны после своих прогонов** (общие исходники; поведение без флага не менялось): прогоны двух игроков и петли относятся к сборкам `CoopSource-5222720d`, `CoopReplica-f7094508`, `CoopInput-40b512bc`, `CoopInputClient-72652b6c`.
- Четвёртый экземпляр (вход в забег) создан `Start-IsaacReplica.ps1 -PersistentGameData … -GameState …`: рабочая папка `Binaries/game-instances/replica-5fa457c0`, профиль `Documents/My Games/IsaacAuthority-5fa457c0`, окно слева внизу.
- Третий экземпляр (общее сохранение) создан `scripts/Start-IsaacReplica.ps1 -PersistentGameData …`: рабочая папка `Binaries/game-instances/replica-1abb0b8a`, профиль `Documents/My Games/IsaacAuthority-1abb0b8a` с копией сохранения хоста (SHA256 `839b891e…`), окно в правом нижнем углу.
- Второй экземпляр создан `scripts/Start-IsaacReplica.ps1`: рабочая папка `Binaries/game-instances/replica-414553eb`, отдельный профиль `Documents/My Games/IsaacAuthority-414553eb`, локальные сохранения, SteamCloud=0 и PauseOnFocusLost=0. Окна помечены HOST / REPLICA. После теста оба процесса работают обычным образом, перехваты отключены; DLL остаются до выхода.

## Последний десинк: уточнение пользователя

**Десинк произошёл ровно при подборе Vibrant Bulb, не при переходе в комнату.** Возврат в соседнюю комнату и разделение лобби — последующие события. Пользователь отдельно уточнил этот порядок; не трактовать его сообщение как сбой перехода.

- Сессия `09_18_2026__19_37_25__Игрок 1`, seed **FCNQ KN1J**, три участника.
- Кадр расхождения **106841**, зарегистрирован на 106844. На скриншоте Lazarus/Игрок 1 поднимает лампочку, активка полностью заряжена.
- Расходится PostVelocity Y Lazarus: локально `0.3324480652809143`, у обоих остальных `0.3035395443439484`. RNG и контрольные суммы общего сохранения совпадают.
- Сообщение `Adding trinket 100` печатается позже при сбросе очереди; оно не переносит момент подбора на время после десинка.
- Архив с логами, скриншотом и разбором: `Binaries/diagnostics/vibrant-bulb-latest/IsaacDiagnostics-20260918-204736-2804ebc8.zip`.
- SHA256 архива: `4f99cbe129c994f7fb7442a64548b0882b4ed97c7f790de7d64be94b1b53491a`.
- [Подробный разбор](docs/vibrant-bulb-desync-20260918-204736.md). В архиве сохранена версия разбора на момент сбора; последующее уточнение пользователя отражено в документах проекта.

Другие сохранённые случаи: [два совпадающих десинка на кадре 38 в стартовой комнате](docs/paired-desync-20260918-180254.md), [ранний Vibrant Bulb, seed KAKG ZR44](docs/vanilla-vibrant-bulb-desync.md). Причина внутри расчёта движения пока не доказана. Гарантированный короткий игровой сценарий/принудительный seed в официальном онлайне не реализован.

## История ранних этапов

Этапы до перехода комнат описаны в своих отчётах и таблице выше: [локальная коррекция игрока](docs/j460-player-step-test.md), [сетевое применение состояния](docs/j460-network-state-test.md), [реплика игрока в двух играх](docs/j460-game-pair-test.md), [жизненный цикл слёз](docs/j460-world-tears-test.md). Их summary лежат в `Binaries/diagnostics/player-step/`, `network-state/`, `game-pair/`, `world/`. Согласование seed ручное: нативный путь через консоль (`Console::RunCommand`, RVA `0x28cdc0`) найден, но безопасная точка вызова перезапуска не установлена. Ранние адаптеры проверяют одного локального игрока и пустой список сетевых устройств; их тесты не запускать поверх онлайн-забега.

## Не сделано и ограничения

- ABI/перехват и разовая запись X проверены в одном одиночном забеге J460. Смена комнаты, уничтожение игрока и длительная работа с коррекциями не проверены. Полная декомпиляция большой функции обновления игрока не завершилась за выделенный лимит.
- Две игры проверены только в пустых стартовых комнатах: loopback, один выбранный игрок, сессия до 30 секунд, пакет до 250 мс. Ограничение скачка 128 пикселей относится к модулю Pair; World имеет отдельные диапазоны WLD1 и проверку контекста комнаты. Полноценные коллизии, бои, переходы и большие коррекции не проверены.
- Реплика подавляет обновление выбранного игрока и созданных ею обычных слёз; остальные сущности мира продолжают собственную симуляцию. У копий слёз отключены столкновения и урон. Нет интерполяции и автоматического согласования seed/сохранений; смена комнаты проверена для одного перехода через одну дверь и покрывает только обычные комнаты сетки в измерении 0.
- Предсказание проверено в пустой комнате; через плохую сеть — только с моделью канала в тесте на одной машине. Ветка «телепорт» сверки вживую не срабатывала. Глубина буфера воспроизведения постоянная (четыре шага), повтора команд в пакетах нет. Предсказывается только движение. Штатное обновление своего игрока имеет локальные последствия (двери, подбор, толчки), они не подавлены. «Свой» игрок зашит как контроллер 1, второй в списке.
- Транспорт — Python-тест на loopback; метки времени пакетов сравниваются с часами получателя (250 мс), что работает только на одной машине. Из действий клиента в живой игре нажимались только движение и стрельба.
- Два игрока проверены только в стартовой комнате без врагов и двери. Состав игроков фиксирован на сессию реплики (присоединение посреди сессии её останавливает); персонаж, здоровье и смерть игроков не сверяются; второй игрок на обеих машинах создаётся присоединением, которому нужно второе устройство ввода.
- Сетевые ID реализованы только для набора до 16 стандартных слёз `2.0.0`; враги комнаты (до 32) сопоставляются по InitSeed без ID; игроки (до 4) — по месту в списке. Нет синхронизации новых врагов и снарядов, здоровья игрока, предметов, наград, сохранений, переподключения и миграции хоста; нет интернет-транспорта и аутентификации — всё проверено только на loopback, сессии до 30 секунд.
- Старый внешний state reader даёт best-effort чтение. Source-модули публикуют данные игрока и поддерживаемых слёз после обновления игрока через счётчик поколений; **границы полного шага и атомарного снимка мира по-прежнему нет**.
- Старые issues, EID-миграция и прочие замечания исследованы в [issue-triage.md](docs/issue-triage.md); не считать все перечисленные проблемы исправленными.

## Состояние рабочей копии

**Публикация (20 сентября 2026, по слову пользователя): <https://github.com/deaddarkus4/Isaac-Authority>, ветка `main`, релиз `v0.1.0` с архивом установки.** Рабочий репозиторий начинался как клон чужого (`xADDBx/Isaac-Online-Modded`, теперь remote `upstream`, отправка отключена): его `master` и папки (патчер, сторонние моды без лицензии) — не наши, и публиковать их нельзя. Поэтому наружу идёт не ветка как есть, а **`python scripts/Publish-IsaacAuthority.py`**: во временном клоне переписывает наши коммиты поверх `master` так, что в них остаются только наши пути (`native/`, `scripts/`, `docs/`, `reverse/`, статус, наш README), отрезает чужую историю, вычищает имена игроков из всех версий текстовых файлов (список — неотслеживаемый `Binaries/publish-names.txt`; без него публикации нет) и отправляет как `main`. Переписывание детерминировано, повторная публикация — обычный fast-forward; хеши опубликованных коммитов не равны рабочим. **Обычный `git push` в `origin` отключён намеренно** (адрес отправки — заглушка): ветка как есть вынесла бы чужое и старые версии отчётов с никами. Релиз: `scripts/New-IsaacAuthorityRelease.ps1 -Name IsaacAuthority-<версия> -Commit <опубликованный хеш>`, затем `gh release create`. Публиковать и выпускать — только по явной просьбе пользователя.

Работа закоммичена в ветке **`authority-prototype`** (от `master`, коммит `a90af38`). Пользовательская папка **`Upgrader/` не относится к этой работе, не менять и не включать в коммиты** — она единственная остаётся неотслеживаемой. `Binaries/` содержит локальные сборки, дампы, отчёты живых прогонов и исследовательские артефакты, игнорируется Git.

Основные ссылки: [интеграция J460](docs/authority-j460-integration.md), [живое чтение и UDP-мост](docs/j460-live-state-reader.md), [сетевой стенд](native/IsaacAuthority/README.md), [диагностический регистратор](docs/native-recorder.md).
